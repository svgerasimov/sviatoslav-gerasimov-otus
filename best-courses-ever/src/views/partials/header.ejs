<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8">
  <title>
    <%= title %> | Best Courses Ever
  </title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">

  <header class="bg-slate-800 text-white">
    <div class="container mx-auto flex items-center justify-between p-4">
      <a href="/" class="font-semibold text-lg">Best Courses Ever</a>
      <nav class="space-x-4">
        <% if (user) { %>
          <span>Привет, <%= user.name %>!</span>
          <a href="/logout" class="border border-white/60 px-3 py-1 rounded hover:bg-white/10">
            Выйти
          </a>
          <% } else { %>
            <a href="/login" class="border border-white/60 px-3 py-1 rounded hover:bg-white/10">
              Войти
            </a>
            <% } %>
      </nav>
    </div>
  </header>

  <main class="flex-1 container mx-auto p-4">